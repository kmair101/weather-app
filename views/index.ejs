<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weather App</title>
  <link rel="stylesheet" href="/public/css/style.css">

  <script type="text/javascript">
    function confirmReset(event) {
        var confirmation = confirm("Are you sure you want to remove all applicants?");
        
        if (!confirmation) {
            event.preventDefault();
        }
    }
  </script>
</head>
<body>
  <div class="container">
    <h1>Weather App</h1>
    <form action="/get-weather" method="POST">
      <input type="text" name="city" placeholder="Enter city" required>
      <button type="submit">Get Weather</button>
    </form>

    <h2>Recent Weather Queries:</h2>

    <ol>
      <% weatherRecords.forEach(function(record) { %>
        <li><strong><%= record.city %></strong>: <%= record.temperature %>°C, <%= record.condition %> </li>
      <% }); %>
    </ol>

    <form action="/remove" method="POST" onsubmit="confirmReset(event)">
      <button type="submit">Remove All Queries</button>
    </form>

  </div>
</body>
</html>
